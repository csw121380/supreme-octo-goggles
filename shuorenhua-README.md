# 说人话：中文 AI 味清理 Skill

**别让模型替你装腔。**

给 Codex、Claude Code、Cursor、ChatGPT 和自建 agent 用。

改聊天、技术同步、README、论坛帖和中文长文：先保住事实，再把那股"一眼 AI"的腔调降下来。

## 概览

`说人话` 专治那种"每个字都对，但一看就不是你写的"中文。它不把空话包装得更漂亮，也不替你编新事实；它先护住版本、命令、责任和证据，然后把模板感、表演感和语域混搭压下去。

它适合这些场景：

| 场景 | 它会做什么 |
|------|----------|
| 日常聊天 | 删掉过度承接、推销式结尾和工程汇报腔，保留口语感 |
| 技术状态同步 | 保住事实、版本、命令、报错和责任归属，压低套话 |
| README / release note | 先讲清楚项目、变更、验证和限制，不写发布宣言 |
| 论坛帖 / issue 回复 | 像维护者在认真沟通，不像客服公告或营销稿 |
| 中文长文 | 句内清理保住节奏，整句空话列清单交你确认 |

## 改写前后对比

### 聊天答疑

**改写前**
> 你说的很对，这个问题一针见血。一句话总结：核心逻辑是先把流程跑通，再谈优化。方案我发你，已经测试通过，希望这对你有帮助。

**改写后**
> 对，问题就在这：先把流程跑通，再谈优化。方案我发你。文档要不要一起弄？

### 发版说明

**改写前**
> 本次版本是一次面向真实场景的系统性升级。我们不仅全面优化了改写体验，更通过全新的能力矩阵稳稳兜住了用户多重诉求。

**改写后**
> ## v1.8.0
>
> - 新增 `references/scene-packs.md`，覆盖 README、release note、forum post
> - 增加 8 条回归用例
> - 这版不做 Voice Calibration；推迟到 v1.9

### 项目介绍

**改写前**
> 在当今快速发展的人工智能时代，如何打造一个真正赋能开发者的工具，已经成为业界不容忽视的关键议题。

**改写后**
> AI 工具很多，但改完的中文常常还留着套话。这个项目专门清这些残味：过度承接、工程师腔、翻译腔、无源权威和自我拔高。

## 核心原则

### 先保信息，再谈风格

改写流程固定六步：

1. **判场景** - `chat / status / docs / public-writing`
2. **划保护片段** - 数字、版本、命令、路径、报错、引用、责任归属先锁住
3. **判命中强度** - `Tier 1 / 2 / 3`（问题严重度）
4. **定改写力度** - `minimal / standard / aggressive`（改写程度）
5. **定编辑范围** - `structural / bounded / in-place`（能删到什么程度）
6. **按规则改写** - 保真回读 → 清理残留味

### 场景决定力度

| 场景 | 默认力度 | 处理策略 |
|------|---------|----------|
| `chat` | 轻 | 只砍明显套话，不把聊天改成公文 |
| `status` | 中 | 保留动作、状态、阻塞点和下一步 |
| `docs` | 中 | 技术表达优先，保真为主 |
| `public-writing` | 重 | 全规则扫描，语域一致为主 |

### 长文不缩水：三档编辑范围

| scope | 删整句吗 | 适用 |
|-------|---------|------|
| `structural` | 自由删并重排 | 短文、明确要重写 |
| `bounded` | 整句空话进删除清单，删多少你拍板 | `public-writing` 长文 |
| `in-place` | 一句都不删，只句内降调 | 明确要求完全原样 |

## 常见问题类型

### 1. 开场套话 / 谄媚

删掉：`值得注意的是`、`让我来为你解释`、`希望这对你有帮助`、`你说得很对`

### 2. 空总结收尾

删掉：`综上所述`、`归根结底`、`本质上`、`At the end of the day`

### 3. 商业黑话

改法：
- `赋能` → 给、提供
- `抓手` → 方法、办法
- `闭环` → 完成、解决
- `兜住` → 保证、守住
- `落盘` → 落实、确认

### 4. 无源引用

改法：删掉`研究表明`、`数据显示`、`experts say`，直接陈述事实或补具体来源

### 5. 翻译腔

改法：缩短主语和动作，少用长定语链、被动堆砌、`基于……`、`通过……来……`

### 6. 过度接住

删掉或改法：`你不是敏感`、`你只是...`、`你问到了问题的核心`这类身份认证式夸奖

## 安装方式

### Claude Code

在 Claude Code 对话里运行：

```text
/plugin marketplace add MrGeDiao/shuorenhua
/plugin install shuorenhua@shuorenhua
```

装好后说「把这段去 AI 味」就会命中。

### Codex

克隆后单次使用：

```bash
git clone https://github.com/MrGeDiao/shuorenhua.git && cd shuorenhua
codex exec -C . "读取 ./SKILL.md，按其中规则改写以下文本：……"
```

### Cursor / Windsurf

下载 `SKILL.md` 到项目，在 `.cursor/rules` 或配置里引用。

### 先试效果（无需安装）

访问 [说人话 GPT](https://chatgpt.com/g/g-6a5829b1163481919e1e45851f6bc709-shuo-ren-hua)（ChatGPT Plus），完整规则已内置，贴文本即可看效果。

## 只标注不改写

在指令里加一句：`按 annotation mode 只标注不改写`

输出会列出最重要的 1-5 个问题点，每个包含：
- 问题族（例如：开场套话 / 无源引用）
- 触发点（命中的词或句子）
- 建议动作（删掉 / 换表达 / 补来源）
- 是否建议改写：是 / 否

## 改写后的方向

- 具体动作优先于抽象拔高
- 真主语和真动作优先于姿态层
- 允许轻微不对称，不把每句都抛光成同一种腔
- 按场景校准，不把聊天改成公告，也不把文档改成段子

## 回读检查

### Pass 1 - 保真回读

提交改写前检查：

1. protected spans 是否漂了？
2. 信息是否丢失？
3. 语域是否统一？
4. 术语是否失真？
5. 删改后是否出现生硬断裂？

### Pass 2 - 残留味审核

只有第一遍已保住事实、但仍有轻微 AI 味时才做。只允许：

- 删一个残留开场或收尾
- 合并两句过匀的事实句
- 把一句 narrator / 空泛判断压回直接表达

不允许：

- 重写全文
- 补原文没有的事实
- 改掉术语、参数、命令或责任归属

## 官方仓库

完整规则、参考资料、评测数据和贡献指南见：

**GitHub: https://github.com/MrGeDiao/shuorenhua**

## 许可

MIT
