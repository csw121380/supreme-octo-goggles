---
name: shuorenhua
description: 检查和清理中英文文本里的 AI 套路，适用于"去 AI 味""说人话""自然一点""别像模板"这类改写和审稿需求；按场景分档、按强度分级、保护术语和责任，改完可直接发。
---

# 说人话

把文本从"像模型在表演写作"拉回"像具体人在当前场景下表达"。

这份 skill 不是敏感词替换器，也不是反技术、反抽象、反专业。它的目标是减少模板感、表演感和语域漂移，同时保住事实、术语和责任主体。

## When to use

在下面这些需求里使用：

- 用户明确说"去 AI 味""说人话""自然一点""别像模板""别太像 ChatGPT"
- 需要改写中文或英文 `chat`、`status`、`docs`、`public-writing`
- 需要先判断文本该轻改、中改还是重改

在下面这些需求里不要硬套：

- 用户要逐字翻译、保留原文风格、仿官方模板或仿特定品牌 voice
- 文本主要是代码、日志、命令、配置、接口名、报错
- 用户要的是事实校对，不是风格改写

## Core stance

- 去 AI 味，主要处理的是模板感、收束腔、虚假主语、语域混搭和表演性技术腔。
- 保留技术性。专业词、系统主语、事故复盘用语、PRD/发布说明中的术语默认可保留。
- 优先保信息，再谈风格。任何改写都不能新增事实、删核心事实或改变责任主体。
- 不用机械同义词替换表。默认可以删句、并句、降调、换主语、去总结式收尾；如果进入 `in-place` scope，就只做句内改写。
- 短语表默认只列代表项，不追求穷举所有变体。遇到新口癖，先按现有模式归类，再决定要不要补词。

## Execution order

按固定顺序做，不要跳步：

1. 判场景：`chat / status / docs / public-writing`
2. 查禁改项：先划 `protected spans`，看有没有必须保留的术语、系统主语、引用原文、命令或正式语体
3. 判 Tier：`Tier 1 / Tier 2 / Tier 3`，按问题命中强度判断，不要把 Tier 当作改写力度
4. 再判档位：`minimal / standard / aggressive`
5. 判 scope：`structural / bounded / in-place`，判断这次能删到什么程度
6. 先执行本文件里的最小规则；按问题类型补看参考资料
7. 回读拆成两步：先做保真回读，再按需做残留味回读
8. 输出：默认只给单一推荐版本；用户明确要求"先标问题，不改写"时切到 `annotation mode`

## 场景检测

先判主场景，再处理局部问题。

### `chat` - 日常对话

信号：短回复、日常对话、协作沟通、评论、即时反馈
默认档位：`minimal` - 只砍明显套话，不把聊天改成公文

### `status` - 状态同步

信号：站会更新、进度同步、复盘摘要、汇报式状态说明
默认档位：`minimal` 或 `standard` - 保留动作、状态、阻塞点和下一步

### `docs` - 技术文档

信号：操作文档、技术说明、接口说明、FAQ、事故复盘
默认档位：`minimal` - 技术表达优先，保真为主

### `public-writing` - 公开文本

信号：公众号、小红书、公开帖、对外文章、观点写作
默认档位：`standard` - 全规则扫描，语域一致为主

## 改写强度级别

### `minimal` - 轻改

适用于：文本本身基本自然，只需去掉局部模板感、收尾腔和多余修辞。

默认动作：
- 删掉空总结
- 把过度抬高的语气压回常规
- 把"像在解释自己会写作"的句子压回事实句

### `standard` - 中改

适用于：有明显 AI 腔或语域混搭，但信息骨架是好的。

默认动作：
- 统一语域
- 改掉工程师表演腔、商业黑话、narrator 腔
- 必要时并句或换主语

### `aggressive` - 重改

适用于：`Tier 1` 命中密集，或 `Tier 1 + Tier 2` 叠加后整段呈现强模板感或强表演感。

限制：只有在 `Tier 1` 明显密集才允许

## 命中强度级别 (Tier)

### Tier 1 - 默认替换

常见类型：
- 开场套话、总结式收尾、谄媚句
- 明显商业黑话、自媒体流水线用语、表演性工程师腔
- 过度接住式共情、替用户做心理判断、郑重预告
- 英文里的 sycophantic openers、significance inflation、business jargon

### Tier 2 - 条件替换

常见类型：
- 高频连接词扎堆（单独出现可放行，同段多个时标记）
- 渲染性修饰词扎堆
- 某一类姿态词在同段重复出现

默认处理：保留最贴切的一个，其余改写

### Tier 3 - 可选处理

常见类型：
- `重要 / 关键 / 核心 / 提升`
- `significant / innovative / effective`

默认处理：只在全文密度明显过高时才处理

## 禁改项 (Protected Spans)

以下内容默认优先保留，除非用户明确要求改风格：

- 引用原文、命令、接口名、参数名、字段名、配置项
- 日志、报错、技术术语
- postmortem / incident / PRD / release note 中的专业术语
- 承载关键事实的抽象句

不要为了"像人"把文本改得更假。专业文本可以专业，关键是别模板化、别表演化。

## 常见问题类型

### 1. 开场套话 / 谄媚

删掉："值得注意的是"、"让我来为你解释"、"希望这对你有帮助"、"你说得很对"

### 2. 空总结收尾

删掉："综上所述"、"归根结底"、"本质上"、"At the end of the day"

### 3. 二元对比骨架

改法：`不是 X，而是 Y` 多数删前半句，直接说 `Y`

### 4. 无源引用

改法：删掉"研究表明"、"数据显示"、"experts say"，直接陈述事实或补具体来源

### 5. 商业黑话

改法：
- `赋能` → 给、提供
- `抓手` → 方法、办法
- `闭环` → 完成、解决
- `兜住` → 保证、守住
- `落盘` → 落实、确认
- `leverage` → 利用、借助

### 6. 过度接住 / 心理判断

删掉或改法："你不是敏感"、"你只是..."、"你问到了问题的核心"这类身份认证式夸奖

### 7. 翻译腔

改法：优先缩短主语和动作，少用长定语链、被动堆砌、`基于……`、`通过……来……`

## 改写前后示例

### 示例 1：聊天答疑

**改写前**
> 你说的很对，这个问题一针见血。一句话总结：核心逻辑是先把流程跑通，再谈优化。我可以直接给你一版绝对没问题的最终方案，已经测试通过，希望这对你有帮助。

**改写后**
> 对，问题就在这：先把流程跑通，再谈优化。方案我发你。文档要不要一起弄？

### 示例 2：发版说明

**改写前**
> 本次版本是一次面向真实场景的系统性升级。我们不仅全面优化了改写体验，更通过全新的能力矩阵稳稳兜住了用户在 README、release note、论坛长帖上的多重诉求。

**改写后**
> ## v1.8.0
> 
> - 新增 `references/scene-packs.md`，覆盖 README、release note、forum post
> - `evals/benchmark.md` 增加 8 条回归用例
> - 这版不做 Voice Calibration；推迟到 v1.9

### 示例 3：项目介绍

**改写前**
> 在当今快速发展的人工智能时代，如何打造一个真正赋能开发者的工具，已经成为业界不容忽视的关键议题。

**改写后**
> AI 工具很多，但改完的中文常常还留着套话。这个项目专门清这些残味：过度承接、工程师腔、翻译腔、无源权威和自我拔高。

## 回读检查表

### Pass 1 - 保真回读

提交改写前，检查这 5 项：

1. protected spans 是否漂了？
2. 信息是否丢失？
3. 语域是否统一？
4. 术语是否失真？
5. 删改后是否出现生硬断裂？

### Pass 2 - 残留味审核

只有在第一遍已经保住事实、但读起来还有轻微 AI 味时，才做第二遍。第二遍固定只查这 5 件事：

1. 开场残留：还在用 `结论先说 / 值得注意的是` 这类提示层
2. 总结残留：还在用 `总的来说 / 归根结底` 这类空收尾
3. narrator 残留：还在解释"这说明了什么"
4. 空泛判断残留：还在写 `方向是对的 / 意义重大`
5. 句长过匀：每句都差不多长，像被统一抛光过

## 输出格式

### 默认模式

输出单一推荐版本，不输出审稿过程或多版本比稿。

### 标注模式 (annotation mode)

只有用户明确要求下面这类事情时才启用：

- `先别改，先标问题`
- `这段哪里像 AI`
- `只做诊断 / 审稿 / 标注`

输出格式：最重要的 1-5 个问题点，每个包含：
- 问题族：例如 `开场套话 / 无源引用 / 工程师腔`
- 触发点：点明命中的词、结构或句子
- 建议动作：删掉、换成具体表达、补来源、保持不动
- 是否建议改写：是 / 否

## 完整规则参考

这份 SKILL.md 可以单独兜底完成基础改写。完整模式通常配合参考资料使用，包括：

- 场景禁改表
- Protected Spans 清单
- 中文禁用短语表
- 英文禁用短语表
- 结构反模式
- 严重度分级
- 微操作手册
- 边界案例集
- 改写示例

更多详情见：https://github.com/MrGeDiao/shuorenhua
